#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - print single no base 10
 * Return: 0 (Success)
 */
int main(void)
{
int num;
while (num < 10)
{
printf("%d", num);
num += 1;
}
printf("\n");
return (0);
}
